[profile.default]
src = "src"
out = "out"
libs = ["lib"]

solc_version = "0.8.24"

remappings = [
  "forge-std/=lib/openzeppelin-contracts/lib/forge-std/src/",
  "openzeppelin-contracts/=lib/openzeppelin-contracts/",
  "@ccip/=lib/ccip-contracts/contracts-ccip/src/v0.8/",
]

# Keep this minimal for now; we will add remappings/deps once CCIP contracts are added.
