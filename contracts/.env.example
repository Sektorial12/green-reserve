# Used by Foundry scripts in ./script/

# Deployer private key (0x... or decimal). Never commit real key.
PRIVATE_KEY=

# Sepolia deployment needs destination receiver address (Base Sepolia)
BASE_RECEIVER=

# Receiver allowlist configuration needs Sepolia sender address
SEPOLIA_SENDER=

# Optional: CCIP gas limit used in GreenReserveCCIPSender
CCIP_GAS_LIMIT=300000
